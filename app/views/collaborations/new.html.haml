%h1 Add Collaborator

= form_for [@wiki, @collaboration] do |f|
  .form-group
    = f.label :user
    = f.select :user, options_for_select(User.all{ |u| [u.username, u.id] })
  .form-group
    = f.submit "Add Collaborator", class: 'btn btn-success'